/**
 * The colours the pane and the deck draw text in, by the names of Claude Code's own theme. A theme key is drawn
 * in the person's theme, dark or light, so a green that reads on one background does not vanish on the other; the ANSI
 * names (`cyan`, `yellow`) are whatever the terminal makes of them, and yellow or cyan text is hard to read on white.
 */
export const HEAD = 'suggestion'
export const GOOD = 'success'
export const BAD = 'error'
export const WARN = 'warning'
/** The one accent: what to do next. */
export const ACCENT = 'claude'
